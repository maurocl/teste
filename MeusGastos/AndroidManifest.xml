<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.mltech"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".MeusGastosActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Toda activity tem que ser declarada no manifest -->
        <activity android:name=".FormCategoria" />

        <!-- Toda activity tem que ser declarada no manifest -->
        <activity android:name=".FormGasto" />

        <!-- Toda activity tem que ser declarada no manifest -->
        <activity android:name=".ListaCategorias" />

        <!-- Toda activity tem que ser declarada no manifest -->
        <activity android:name=".ListaGastos" />

        <!-- Toda activity tem que ser declarada no manifest -->
        <activity android:name=".Login" />

        <!-- Toda activity tem que ser declarada no manifest -->
        <activity android:name=".FormDatas" >
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>